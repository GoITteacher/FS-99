// foreach

/* const arr = [6, 7, 2, 4, 8, 9, 3, 1, 5];

arr.forEach((item) => {
  console.log("Saved to Database", item);
});

arr.forEach((item, idx) => {
  console.log(`${idx + 1}) ${item}`);
});
 */
